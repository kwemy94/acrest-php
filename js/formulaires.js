/* Fonction permettant de modifier un champ mal rempli */
function error_sur_un_champ(champ, erreur){
	if (erreur) {
		champ.style.backgroundColor = '#fba';
	} else{
		champ.style.backgroundColor ='';
	}
}

/* verification des champs de type texte */

function champ_texte(champ){
	if (champ.value.) {}
}